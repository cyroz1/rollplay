{
  "name": "rollplay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-native FL Studio project visualizer, MIDI exporter, and MP4 renderer.",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=18" }
}
